#pragma once

#include "../images/ImageLib.h"

namespace PTG
{
    namespace Base_figures
    {
        PTG::greyscale star(int size, float power);
        PTG::greyscale gaussian(int size);
        PTG::greyscale square(int size);
    }
}
