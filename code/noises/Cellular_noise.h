#pragma once

#include "../images/ImageLib.h"

namespace PTG
{
    namespace Noises
    {
        PTG::greyscale celluar(int size, int detail, int mode, int dist_mode = 0, int cell_coef_mode = 0, float dist_power = 1);
    }
}
